nums = [20, 20 ,50, 25, 85];
function fors(df) {
    for (let i = 0; i < nums.length; i++) {
        const shakil = nums[i]; 
        return shakil; 
    } 
    var ss = fors(df);
console.log(ss);
} 

nums = [12, 25, 30, 28, 45, 8, 26];
function isEvenAll(nums) {
    for (let i = 0; i < nums.length; i++) {
        const shakil = nums[i];
        isEven(shakil);
    }
}